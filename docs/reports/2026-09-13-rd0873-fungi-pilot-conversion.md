# RD-0873 Fungi pilot conversion — 2026-09-13

Status: **PASS_BOUNDED_PILOT**

The owner authorized a bounded `.fungi` conversion run. The first wave was
kept to four already selected scalar symbol scopes, one source file per step,
profile `scalar-1`, concurrency `1`, and zero retries. The TypeScript files
remain the active shadows and no runtime consumer was switched.

The four product-tree twins already existed from the earlier bounded wave. The
run therefore revalidated their exact bytes and behavior at the current
Galerina head rather than rewriting identical files. That distinction is
recorded so an earlier proposal is not presented as a new generated artifact.

## Current binding

- Galerina HEAD: `675e1048304b11e109b1de4f68af97ebc64f5949`
- Galerina tree: `b20138180928ed9d787d157d1faedbe86050be64`
- Profile: `scalar-1`
- Scope: `SYMBOLS`; whole-file replacement: `false`
- Per-step limits: one symbol/source file, 65,536 input bytes, 65,536
  output/evidence bytes, 600,000 ms, concurrency `1`, retries `0`
- Wave limits: four symbols and four source files; chapter aggregate only
  after all four items

## Converted and verified

| TypeScript shadow | Product-tree target | Result |
| --- | --- | --- |
| `packages-ts/galerina-core-config/src/index.ts#isEnvironmentMode` | `packages/fungi/products/galerina/rd0873-core-config/environment-mode.fungi` | strict check PASS; differential PASS |
| `packages-ts/galerina-core-runtime/src/structured-await.ts#isTerminalScope` | `packages/fungi/products/galerina/rd0873-core-runtime/terminal-scope.fungi` | strict check PASS; differential PASS |
| `packages-ts/galerina-core-tasks/src/load-tasks.ts#isTaskEffect` | `packages/fungi/products/galerina/rd0873-core-tasks/task-effect.fungi` | strict check PASS; differential PASS |
| `packages-ts/galerina-data-model/src/index.ts#isResponseSafeClassification` | `packages/fungi/products/galerina/rd0873-data-model/response-safe-classification.fungi` | strict check PASS; differential PASS |

Focused evidence:

- Four strict type/governance checks: **4 passed, 0 failed, 0 skipped**.
- Four package conversion suites: **8 passed, 0 failed, 0 skipped**.
- The differential vectors include every admitted literal and hostile surplus
  strings such as empty, case-changed, whitespace-padded and NUL-suffixed
  values.

## What succeeded

The pure scalar decisions preserve their closed String domains and explicit
wildcard refusals. The product targets are in the intended
`packages/fungi/products/galerina` tree, and source/target hashes plus the
current head/tree are recorded in the machine-readable receipt beside this
report. The TypeScript shadows, package tests and rollback route remain
available. No consumer, production authority, profile admission or TypeScript
retirement changed.

## Issues found

The previous four-item authority manifest is bound to an older head and was
not reused. Galerina does not currently provide a general TypeScript-to-Fungi
emitter for these functions, so this wave depends on manually authored twins.
Current-head checked snapshot/GIR, independent SLIDE/VOK re-derivation and an
independent review were not generated by this local pilot. Those omissions
leave physical admission and production claims closed.

## Improvements recorded

Keep product outputs separate from `packages-ts`, retain TypeScript shadows,
and bind each step to an exact source/target digest. Continue the controlled
wave with one symbol at a time and aggregate only at package-chapter closure.
Use a fresh manifest for the next scope instead of extending this receipt, and
obtain independent semantic and physical review before any consumer switch or
retirement decision.

The complete machine-readable record is
`docs/independent-audits/2026-09-13-rd0873-fungi-pilot-conversion.json`.
