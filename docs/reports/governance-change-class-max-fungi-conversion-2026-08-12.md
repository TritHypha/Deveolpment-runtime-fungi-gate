# Governance change-class maximum Fungi conversion

## Result

The compiler's private `maxClass` decision now has one package-owned Fungi
translation and independent physical SLIDE/VOK evidence. The translation is
reference-only: TypeScript remains the executing governance-diff layer and no
consumer has switched.

## Exact source custody

| Artifact | SHA-256 |
|---|---|
| `packages-galerina/galerina-core-compiler/src/governance-diff.ts` | `7b368f689a822b7a34e4a7101010dc113b5db72acd40c8952f1e7a9e94090d6c` |
| `packages-galerina/galerina-core-compiler/src/self-hosted/governance-qualifier-escalation.fungi` | `b9e650ac5a2b5254b3e9b2e8d71e194d772e444fd5afe999745b56ee916367d9` |

The RED proof is committed at `f462fdbe`, the first Fungi implementation at
`0860b02d`, the verified-depth correction at `844ac359`, and the physical proof
at `11a23010`. Independent SLIDE remains pinned at `6de4d91`.

## Semantic proof

The admitted flow preserves the source order `neutral < tightening < expansion
< experimental` for every one of the 16 typed pairs and preserves the source's
left-biased tie. At the physical String border, every unrecognised value is
normalized to conservative `experimental`; no unknown authority label can be
returned.

The first physical probe refused because the direct
`maximum -> rank -> normalize` call chain had depth three while SLIDE's verified
scalar profile permits depth two. The fix did not raise that ceiling: the
maximum flow uses direct ordered checks over two normalized inputs. It remains
strict-clean and the differential proof passes **2/2**.

Independent SLIDE publishes and re-admits one physical `.slide`, then verifies
the complete declared-plus-hostile String matrix through typed VOK receipts
(**1/1**, zero skips). Wrong argument counts and types, invalid Unicode,
inadequate work, source mutation, receipt fields, every safe-value envelope
byte, and artifact mutation all refuse. The proof pins registry
`slide.registry.executable-gir.v2c-bounded-wide-control-flow.v1` with digest
`d805dae4b822392e5092126ce4f0fb27e8bfa6aa2de8862ee88e09e23eed43cc`.

## Language constraints

The Fungi source contains no null, NaN, `else if`, `else`, exception syntax,
`for`, `while`, or `loop`. It invents no syntax, widens no registry, and raises
no compiler or execution limit.

## Closure and authority boundary

The compiler package passes **6,382/6,382 across 1,259 suites** with zero
failures or skips. The monitored canonical owner completed with recorded exit
code 0: **100/100 packages and 9,604 tests in 310.6 seconds**. Golden is current
at **11/11 checked examples and 11/11 execution vectors**. Retirement derives
**1,447** executable-family paths and **131** source Fungi assets; its refreshed
owner and staged-index anti-neutering proof are green.

`governance-diff.ts`, `CLASS_RANK`, `diffGovernance`, every source classifier,
and every consumer remain active. This slice grants no consumer switch,
TypeScript retirement, bootstrap fixpoint, signing, release, production,
durability, or general source-family authority. Crash-linked full tooling,
normal phase-close, graph-all, and monolithic memory evaluation remain
excluded, so repository-wide closure is `UNKNOWN`.
