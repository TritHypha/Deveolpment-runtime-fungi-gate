@version 1
/// example: 416-target-fallback-missing
/// level: 8
/// concept: compute target without fallback
/// expected_diagnostics: FUNGI-TARGET-001
/// test_status: stable

secure flow runModel(input: Tensor<Float32, [1, 128]>) -> Tensor<Float32, [1, 10]>

contract {
  intent {
    "Demonstrate that a non-CPU compute target requires a deterministic fallback."
  }

  effects {
    ai.inference
  }
}
{
  compute target npu {}
  return NeuralModel.run(input)?
}
