# Galerina Roadmap — 2026-07-25

> **Historical and superseded.** Do not use this file as a current task queue.
> The former production DSS/Wasmtime sidecar is retired. Use
> [the current roadmap](roadmap-2026-07-29-galerina-beta-v1-to-slide.md) and
> [the runtime reconciliation](reports/roadmap-legacy-runtime-reconciliation-2026-08-04.md).

**State anchor:** `main` @ `ef3a746e` (ahead of origin; owner pushes). Beta — nothing here is
"shipped/frozen/released". Every number below is gate-evidenced from a run this cycle, not hand-typed.

> Living forward-view for 2026-07-25; supersedes `roadmap-2026-07-24.md`. Per-item live state lives in
> `TODO.md` (head) + the session bridge (`_session-bridge/`). Authoritative session resume:
> `memory/galerina-work-state.md` (cont.64). Owner **re-affirmed full unlock** 2026-07-25 — authorization
> gates lifted; the **evidence gate + R&D §5a countersign remain structural** (a flip may not bless dead code).

## Where we are (this cycle's battery — all green)

| Gate | Result |
|------|--------|
| graph-all | **6/6** — project 6951n/7220e · integrity 0 · kb 17 orphans/0 broken · Hardened Border **97/0** · memory clean · dev-tools 97 pkgs/118 tools/40 proofs |
| tests | **95/95 packages · 7,867 · 0 fail** (compiler 5,104) |
| audits | **5/5** — artifact-drift 0/542 · compiler-stage-twins (**4 authoritative, RD-0528 flip LIVE**) · percent-history · private-doc-leak 0/3917 · path-leak |
| % audit | ship-readiness **97.9%** (95/97 pkgs) · Zero-Trust thesis avg **78%** · build avg **75%** · 20 tracking items; staleness gate GREEN |

⚠ **version.json drift:** the one `[live]` % (Tests) reads version.json = **7,591**, but the live suite is
**7,867**. No auto-writer exists (`run-all-tests.cjs` has none); NOT hand-edited. Reconcile via a proper
version-count regen tool.

## Self-hosting (RD-0528) — the primary track

**Ledger: 4 of 7 stages authoritative** — type-checker · effect-checker · gir-emitter · runtime. All 7 stages at
**R3 byte-parity** (138/138 `wat-p9-*-parity`); interpreter parity 100%; stages RUN (audit-stage-execution baseline 0).

**5th flip — governance-verifier — HELD** (both sessions converged, proposer≠verifier):
- **Q-A (flip dead-code):** 9 of 14 gov codes ride 5 check-flows that are **uncalled + untested** in the `.fungi`
  twin (CG-9.1). Disposition, evidenced by `.ts` call-sites: **STEP + MUTATION are symmetric-dead → remove-safe**;
  **MONO + INHERIT + TRAP have live `.ts` enforcement → wire (grammar), don't delete** (deleting = a permanent
  Tier-2 capability gap). ← the one owner/architecture decision that unblocks the flip.
- **Q-B (parser fail-open):** `parser.fungi` silently drops `emergency`/`parent_policy`/`trap` governance
  constructs (measured: `.ts` rejects/recognizes, `.fungi` accepts clean, construct dropped — 3 distinct parse
  paths). A latent, pre-Tier-2 defect. Harden design settled = shape-match refusal at all 3 sites, one new code
  **FUNGI-PARSE-006**, reusing the FUNGI-PARSE-005 scanner pattern. **Awaiting R&D §5a-confirm (bridge 0293)**
  before any `parser.fungi` edit; build-feasibility already resolved.

**Remaining self-hosting (post-v1):** lexer + parser flips · bootstrap fixpoint (whole-compiler self-compile,
blocked on duplicate export names) · Tier-2 `.ts` retirement (needs I-2 bootstrap seed).

## Next (sequenced)

1. **R&D §5a-confirm** on the Q-B harden (0293) → build FUNGI-PARSE-006 + SUBSET-REFUSAL edge-diff rows + re-pin
   the parser stage hash → R&D §5a of the built change.
2. **Owner Q-A ruling** (remove STEP/MUTATION · wire-vs-document MONO/INHERIT/TRAP) → gov-verifier becomes the
   5th authoritative stage under doc-08 §5a.
3. Version-count regen tool (close the 7,591/7,867 drift so the [live] % is honest).
4. Owner-unlocked backlog now open: Package Standard + publish ladder (#66) · R4 T2–T5 tranches (#131) · the
   larger owner-gated structure (R4/R5/R8).

## Readiness snapshot (from `component-health.mjs`, this cycle)

- **Zero-Trust thesis (78%):** Compiler 100 · Packages 98 · I/O-kernel 72 · Memory 62 · TLSTP 56 — all `[asserted]`,
  remainder is the #143 execution flip (owner), not wiring.
- **Build progress (75%):** Spec/Lexer-Parser/DRCM/CBOR/Stage-B-interp 100 `[asserted]` · Tests 100 `[live]` ·
  Type/Effect-checker 100 `[derived]` · WAT emitter 89 · Runtime 87 · App-framework 72 · PQ&HW 40 · Passive-plans
  35 · AI Tower 30 · Photonic 3. Word rows (no countable ladder): Stage-B WASM execution (R3 all 7 stages) ·
  B8 governed transport (in progress).
- Honesty note: only **Tests** is live and **type/effect-checker** is derived; every other % is asserted debt,
  ratcheted by `audit-percent-evidence.mjs`. A row with no checkable ladder carries a WORD, not a number.
