# Galerina Roadmap — 2026-07-24

**State anchor:** `main` @ `f702c13d` (30 commits ahead of origin; owner pushes). Beta — nothing here is
"shipped/frozen/released". Every number below is gate-evidenced from a run this arc, not hand-typed.

> This is the living forward-view for 2026-07-24; it supersedes the morning snapshot at `40f40903`.
> Per-item live state lives in `TODO.md` (head) + the session bridge (`_session-bridge/`). The
> authoritative session resume is `memory/galerina-work-state.md` (cont.20).

## Where we are (verified this arc)

| Track | State | Evidence |
|---|---|---|
| Compiler self-hosting (RD-0528) | **2 of 7 stages authoritative** — `type-checker.fungi` + `effect-checker.fungi`; other 5 differential (run through #105) | `audit-compiler-stage-twins` 8/8, "2 authoritative" (verified) |
| **#163 intern-store coupling** | CLOSED — per-stage hashes build-order-independent | prior arc; stage-hash gate green |
| **Declassifier-shadow floor (CWE-501)** | `FUNGI-VALUESTATE-011` LANDED + KB-registered; allowlist EMPTY | `c365fc8d` · KB `aaf5ab0` · `diagnostic-namespace` 3/3 |
| gov-verifier I-3 coverage | **3 → 5 of 14** (GOV-002·VAL-001·VAL-002 + GOV-004·GOV-005) | I-3 corpus `54/54`, tranche 5 `f702c13d` (measured) |
| — gov-verifier flip status | **parser-gated**: 9 of 14 codes are parser-blocked (INHERIT/MONO/TRAP/MUTATION/STEP), NOT flip-ready | measured; R&D-confirmed at source, bridge 0208/0209 |
| Three-valued security thesis (S1–S3 + K3 + TYPE-033) | COMPLETE — governance/verdict fail-open family fails CLOSED at check+build; K3-004/005 minted+registered | invariant gate `security-type-codes-invariant.test.mjs` |
| Phase-close drift gates | both GREEN — `artifact-drift` exit 0 (phantom=111, dead=13 A-PRIME named-set), `percent-history` exit 0 | this arc |
| DSS sidecar | at un-gated ceiling (M0·M1·F3·F4) | prior arc |

## Now — unblocked, mine (no owner/R&D gate)

1. **Self-hosted parser productions for the 5 blocked governance constructs** — `emergency` · `parent_policy`
   · `trap` · mutation-policy (`only_decrease`/`max_change`) · `step`. The self-hosted parser has no
   production for these, so 9 of 14 gov-verifier codes can never fire through the pipeline. Building them
   unblocks the gov-verifier I-3 charter (5→14) AND advances the parser stage itself. Greenfield, R&D-unclaimed
   (bridge 0209). ⚠ parser.fungi is a differential stage → each edit needs the full R3-parity re-prove +
   hash-regather ceremony; scope one construct per unit, never split across ticks.
2. **Per-stage flip evidence packs** for the 5 differential stages (lexer · gir-emitter · runtime · parser +
   the gov-verifier maturity note) — R3-parity + mutation-kill + hash-pin + #105-admit + I-3 oracle, same form
   as the kernel T1/T2 packs. Each pack is a PROPOSAL; the flip itself stays owner-gated (I-4).
3. **Runtime-stage I-3 oracle** — needs an EXECUTION-VALUE oracle (does the self-hosted runtime compute the
   right value?), a different shape from the diagnostics-oracles that cover the checker stages. Unbuilt.

## Next — R&D-gated / coordination

- **gov-verifier flip framing** (R&D 0209): 5/14 reachable ⇒ don't flip as charter-complete. Owner's call at
  I-4 — (a) land the parser productions first, then flip; or (b) flip with an explicit ledger note that 9/14
  codes are parser-dormant. R&D is recording this in the flip-readiness audit.
- R&D re-probes #163 build-order independence (differing build order → identical hashes).
- gir-emitter flip: close the `op:load` oracle-coverage gap, then its pack.
- parser flip: LAST (most consumers) — after its pack + the productions above.

## Owner-gated

- Per-stage authority flips (I-4 condition-form nod, one stage at a time; never self-authorize, never extend
  one nod to another stage).
- DSS Phase-5: F7 audit-before-effect · collapse-conformance · U2 version-floor rollout (needs re-sign).
- R4/R5/R8 · Package Standard R1–R6 · flagship param-admission review.
- Housekeeping: `memory/galerina-work-state.md` is 560 KB+ (S18) — archive/trim the older `cont.*` entries.

## Deferred (needs a prerequisite that does not exist yet)

- **Full `.ts` retirement (tier-2 deletion)** — requires a self-hosted WASM backend; today the 7 stages reach
  lex→parse→check but emit no WASM (the backend is `.ts`). Ties to P9 Stage-B's 4-stage-concat execution
  driver (#56). Also needs the I-2 bootstrap-seed. A real roadmap item, not this arc.
