# Galerina Roadmap — 2026-07-24

**State anchor:** `main` @ `40f40903` (unpushed; owner pushes). Beta — nothing here is "shipped/frozen".
Numbers below are gate-evidenced, not hand-typed.

## Where we are (verified this session)

| Track | State | Evidence |
|---|---|---|
| Compiler self-hosting (RD-0528) | type-checker.fungi **AUTHORITATIVE** (flipped); other 6 stages differential | `47d40382`; stage-twins 8/8 |
| **#163 intern-store coupling** | **CLOSED** — `resetStringTable` wired at the sole emit chokepoint | `40f40903`; stage-hashes now build-order-independent |
| Three-valued security thesis (S1–S3 + K3 family) | **COMPLETE** — full checkTypes governance/verdict fail-open family fails CLOSED at check+build | `8cd45f6f`·`b04f8198`·`719e47bc`·`776c5da5`·`bff5e66f` |
| — invariant gate | recurrence UNREPRESENTABLE (a new gov code un-extracted = RED) | `security-type-codes-invariant.test.mjs` |
| DSS sidecar | at un-gated ceiling (M0·M1·F3·F4) | prior arc |
| Suite / signing path | core-compiler **5008/5008**; kernel ledger **29/29** intact | this session |

Security set now fail-closed: `{TYPE-033, GOV-3VL-003, GOV-3VL-004, K3-001, K3-002, K3-003}`.

## Now — unblocked, mine (no owner/R&D gate)

1. **4-stage flip evidence packs** — #163 removed the hash-coupling blocker, so lexer · effect-checker ·
   governance-verifier · runtime can each get an independent item-(d) hash-pin. Build one evidence pack
   per stage (R3-parity + mutation-kill + hash-pin + #105-admit), same form as the kernel T1/T2 packs.
   Each flip itself stays owner-gated (I-4 nod) — packs are PROPOSALS.
2. **Reconcile 2 phase-close drift gates** — artifact-drift (`phantom 111→112`, `dead 8→13` on regen) +
   `audit:percent-fresh` staleness. Identify the new registry codes, then either remove the spurious
   reference or update the shrink-only baseline with justification.
3. **Mint FUNGI-K3-004 / K3-005** (once R&D confirms the numbers) — the two A9 fail-opens that currently
   ride generic TYPE-004/008 (`v == b`, `return 2 as Verdict`). The invariant gate will force their
   extraction.

## Next — R&D-gated (parked in bridge 0172)

- R&D re-probes #163 build-order independence (build stages in a different order → identical hashes).
- R&D confirms K3-004/005 numbers + registers them.
- gir-emitter flip: close the `op:load` L253 oracle-coverage gap, then its pack.
- parser flip: LAST (most consumers) — after its pack.

## Owner-gated

- Per-stage authority flips (I-4 condition-form nod, one stage at a time; never self-authorize).
- DSS Phase-5: F7 audit-before-effect · collapse-conformance · U2 version-floor rollout (needs re-sign).
- R4/R5/R8 · Package Standard R1–R6 · flagship param-admission review.

## Deferred (needs a prerequisite that doesn't exist yet)

- **Full `.ts` retirement (tier-2 deletion)** — requires a self-hosted WASM backend; today the 7 stages
  reach lex→parse→check but emit no WASM (the backend is `.ts`). Ties to P9 Stage-B's 4-stage-concat
  execution driver (#56). A real roadmap item, not this arc.
