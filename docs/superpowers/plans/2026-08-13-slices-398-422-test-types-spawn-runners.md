# Slices 398-422 test types, spawn and runner foundations plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `superpowers:subagent-driven-development`. Workers are read-only; the root
> agent owns repository writes, verification and commits.

**Goal:** Account 25 previously ungoverned `galerina-test` scopes without
mistaking erased TypeScript, path constants, child-process control or build
evidence parsing for admitted Fungi/SLIDE authority.

**Architecture:** Split three independent dossiers: erased harness types,
spawn/process control plus runner constants, and compiler-freshness/Git input
evidence. Preserve exact host observations and classify every absent physical
type/effect as a named blocker; create no placeholder Fungi.

**Tech Stack:** TypeScript 5.9, Node.js 24, `node:test`, Galerina `.fungi`, GIR,
SLIDE/VOK, Myco and codebase-memory.

## Global Constraints

- Pin HEAD `e92a8e4aa0b48331875f78084ffc3d3c284862e0` and each full source SHA-256.
- Use codebase-memory first; verify exact source and bounded Myco when graph
  caller or build-point evidence is incomplete.
- Read both private Fungi skills before adjudication; keep them private/unpushed.
- Never author null, NaN, `else if`, throw/try/catch, `for` or `loop` in Fungi.
- Use only bounded Boolean `while`; preserve exact failure and cleanup ordering.
- Create no authorizing or retiring candidate without exact GIR, physical
  `.slide`, independent re-admission and VOK evidence for every type/effect.
- Do not run crash-linked aggregate tooling, phase-close or monolithic memory.
- Repository-wide and adjacent test-MJS closure remain `UNKNOWN`.

---

## Scope

| Slice | Exact scope |
|---:|---|
| 398 | `CheckKind` |
| 399 | `CheckScope` |
| 400 | `CheckResultKind` |
| 401 | `TestCounts` |
| 402 | `CheckResult` |
| 403 | `HarnessOptions` |
| 404 | `UnitOptions` |
| 405 | `ConformanceOptions` |
| 406 | `FidelityOptions` |
| 407 | `SlideOptions` |
| 408 | `E2eOptions` |
| 409 | `AllOptions` |
| 410 | `SpawnOutcome` |
| 411 | `DEFAULT_TIMEOUT_MS` |
| 412 | `runNode` |
| 413 | `UNIT_RUNNER` |
| 414 | `R6_PARITY` |
| 415 | `FIDELITY_DIFFERENTIAL` |
| 416 | `COMPILER_DIST` |
| 417 | `COMPILER_BUILD_EVIDENCE` |
| 418 | `COMPILER_PACKAGE` |
| 419 | `GALERINA_CLI` |
| 420 | `COMPILER_EVIDENCE_SCHEMA` |
| 421 | `compilerFreshnessFailure` |
| 422 | nested `gitPaths` |

### Task 1: Pin sources and focused evidence

- [x] Pin `types.ts` SHA-256
  `C99F24250846C5523F8D1D4E4B4EE2EE79AF99AE3431D2BE8F2D7BCA02550724`.
- [x] Pin `spawn.ts` SHA-256
  `E64741FAB4D1441F4F81045CA8554908B1A4D35217F61A99FA0E3CB46B735E25`.
- [x] Pin `runners.ts` SHA-256
  `A9F7395D08F1DDC97087B8E8BBF163EE10C3C72684B4FB889BB0F456B13FD645`.
- [x] Establish fresh focused package typecheck/test evidence and distinguish
  source evidence from an existing `dist` build.
- [x] Reconcile callers, prior receipts, package assets and exact
  Fungi/GIR/SLIDE/VOK evidence.

### Task 2: Produce three independent dossiers

- [x] Record exact type erasure, records, aliases, numeric widths, arrays,
  callbacks, environment, clocks, child processes, streams, timeouts, signals,
  filesystem/Git reads, JSON and cryptographic digest behavior.
- [x] Classify erased declarations as `NO_RUNTIME_BEHAVIOR`; create no enum or
  record placeholder.
- [x] For runtime scopes, record exact blocker, threadability, hostile vectors
  and complete physical/effect admission exit.
- [x] Update either private skill only for a reusable missing rule; otherwise
  record evidence-based `NO_SKILL_UPDATE` in every receipt.

### Task 3: Publish and review governed evidence

- [x] Create 25 exact slice receipts and update the live register, TODOs and
  roadmap without overstating focused tests or physical authority.
- [x] Run receipt audit and focused package lanes.
- [x] Obtain three independent read-only reviews of pins, arithmetic,
  classifications, blockers, threadability, skills and physical refusals.
- [x] Commit authored evidence separately from generated owners.

### Task 4: Fifty-slice maintenance boundary

- [x] Commit the Slice 398-422 receipts after focused checks and review, but
  defer registered owner regeneration to the Slice 447 boundary.
- [ ] Complete Slices 423-447 as the second 25-slice wave.
- [ ] After Slice 447, regenerate registered owners individually in dependency
  order and run the 19-check bounded owner matrix for all 50 slices; do not
  substitute aggregates.
- [ ] Commit the final generated build point, refresh Myco and codebase-memory,
  and prove exact indexed HEAD plus a named symbol lookup at Slice 447.
- [ ] Keep repository closure `UNKNOWN` unless every separate exit is proved.
