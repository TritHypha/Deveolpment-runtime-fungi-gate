# Slices 96-98 verdict-boundary plan

1. Verify the exact TypeScript exports and the existing package-owned
   `authorization-boundary.fungi` source.
2. Run the existing complete direct collapse and authorization proofs across
   all three K3 inputs with zero skips.
3. Run both physical SLIDE/VOK lanes against the pinned sibling SLIDE checkout
   and retain their mutation, hostile-argument, exhaustion and typed-receipt
   evidence.
4. Run the complete Tower-Citizen package lane, which owns the observable
   `decideAtBoundary` callback and diagnostic contract.
5. Publish canonical reports for Slices 96-98, using exact supersession for
   `collapse` and `authorize` and a fail-closed ABI exit for
   `decideAtBoundary`.
6. Update the live register, TODO, assurance status and roadmap; regenerate
   only registered dependent owners in dependency order.
7. Run the governed slice-receipt, canonical-claim, leak and bounded graph
   checks. Do not run crash-linked aggregate lanes.
8. Review both private Fungi skills, keep them private, commit the bounded
   group locally and retain repository-wide closure as `UNKNOWN`.
