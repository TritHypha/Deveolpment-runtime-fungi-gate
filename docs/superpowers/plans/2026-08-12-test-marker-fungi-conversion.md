# Test Marker Fungi Conversion Implementation Plan

**Goal:** Prove the galerina-test CLI's private Boolean marker as an exact
package-owned `.fungi` twin through canonical execution and physical
SLIDE/VOK, without switching or retiring TypeScript.

**Architecture:** Admit one exact symbol in the conserved queue. Add one pure
Boolean-to-String flow, compare its complete input domain to the bound
TypeScript mapping, then compile the exact bytes into one physical `.slide`
and re-admit it through VOK.

## Constraints

- [ ] Authorize only `mark`, not the rest of `cli.ts`.
- [ ] Preserve `true → ✅` and `false → ❌` byte-exactly.
- [ ] Use no null, NaN, `else if`, exception syntax, `for`, `while` or `loop`.
- [ ] Add no effect, capability, contract permission, Hallmark, border grant,
  global vault access or host API.
- [ ] Keep TypeScript and `printHuman` active.
- [ ] Commit locally only; do not push.
- [ ] Do not run full tooling, `graph-all`, normal phase-close or monolithic
  memory evaluation.

## Task 1: Exact queue admission

- [ ] Remove the blocked JSON symbols from current candidate authority.
- [ ] Bind the committed marker design digest to exactly `cli.ts#mark`.
- [ ] Regenerate and check the queue; require zero whole-file and one scoped
  candidate.

## Task 2: Differential RED/GREEN

- [ ] Add `tests/test-marker-fungi-conversion.test.mjs`.
- [ ] Bind the exact private TypeScript decision and package asset manifest.
- [ ] Observe the intended missing-source RED.
- [ ] Add `src/self-hosted/test-marker.fungi` and package loaded-asset entry.
- [ ] Check strict types/governance and prove interpreter plus signed Wasm for
  `true` and `false`.
- [ ] Run the owning galerina-test package.

## Task 3: Physical SLIDE/VOK proof

- [ ] Add `scripts/tests/test-marker-fungi-slide.integration.test.mjs`.
- [ ] Publish and independently re-admit the exact source through SLIDE/VOK.
- [ ] Verify typed String receipts for both marker values.
- [ ] Prove typed-border, custody and physical mutation refusals with zero
  skips.

## Task 4: Governed closure

- [ ] Register the physical test in the governed tooling manifest.
- [ ] Update TODOs, active roadmap, subway and a dated report.
- [ ] Review both public Fungi skills and record the exact disposition.
- [ ] Run only bounded compiler/package/physical and owner checks.
- [ ] Refresh primary graph and Myco at the final commit and verify the marker
  and scoped queue are queryable.
- [ ] Verify a clean tracked worktree and local commits; do not push.
