# Test Marker Fungi Conversion Implementation Plan

**Goal:** Prove the galerina-test CLI's private Boolean marker as an exact
package-owned `.fungi` twin through canonical execution and physical
SLIDE/VOK, without switching or retiring TypeScript.

**Architecture:** Admit one exact symbol in the conserved queue. Add one pure
Boolean-to-String flow, compare its complete input domain to the bound
TypeScript mapping, then compile the exact bytes into one physical `.slide`
and re-admit it through VOK.

## Constraints

- [x] Authorize only `mark`, not the rest of `cli.ts`.
- [x] Preserve `true → ✅` and `false → ❌` byte-exactly.
- [x] Use no null, NaN, `else if`, exception syntax, `for`, `while` or `loop`.
- [x] Add no effect, capability, contract permission, Hallmark, border grant,
  global vault access or host API.
- [x] Keep TypeScript and `printHuman` active.
- [x] Commit locally only; do not push.
- [x] Do not run full tooling, `graph-all`, normal phase-close or monolithic
  memory evaluation.

## Task 1: Exact queue admission

- [x] Remove the blocked JSON symbols from current candidate authority.
- [x] Bind the committed marker design digest to exactly `cli.ts#mark`.
- [x] Regenerate and check the queue; require zero whole-file and one scoped
  candidate.

## Task 2: Differential RED/GREEN

- [x] Add `tests/test-marker-fungi-conversion.test.mjs`.
- [x] Bind the exact private TypeScript decision and package asset manifest.
- [x] Observe the intended missing-source RED.
- [x] Add `src/self-hosted/test-marker.fungi` and package loaded-asset entry.
- [x] Check strict types/governance and prove interpreter plus signed Wasm for
  `true` and `false`.
- [x] Run the owning galerina-test package.

## Task 3: Physical SLIDE/VOK proof

- [x] Add `scripts/tests/test-marker-fungi-slide.integration.test.mjs`.
- [x] Publish and independently re-admit the exact source through SLIDE/VOK.
- [x] Verify typed String receipts for both marker values.
- [x] Prove typed-border, custody and physical mutation refusals with zero
  skips.

## Task 4: Governed closure

- [x] Register the physical test in the governed tooling manifest.
- [x] Update TODOs, active roadmap, subway and a dated report.
- [x] Review both public Fungi skills and record the exact disposition.
- [x] Run only bounded compiler/package/physical and owner checks.
- [x] Refresh primary graph and Myco at the final commit and verify the marker
  and scoped queue are queryable.
- [x] Verify a clean tracked worktree and local commits; do not push.
