# Slices 168-172 Neutral Bridge Boundary and Maintenance Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Account for the remaining neutral bridge interface surfaces through
`CertificationProfile`, then close the approved 25-slice maintenance boundary
with individual, resumable owners.

**Architecture:** Treat erased TypeScript declarations as
`NO_RUNTIME_BEHAVIOR`, while retaining their exact ABI as prerequisite work for
Fungi consumers. Keep `assertDeterminism` blocked until the complete
`BridgeResult` string/record and JavaScript Error boundary is admitted. After
the slice commit, run graph/index/roadmap owners individually; never invoke the
crash-linked aggregate graph or phase-close lanes.

**Tech Stack:** TypeScript, Node `node:test`, checked Fungi, Myco, individual
Galerina owner tools, Markdown slice receipts.

## Global constraints

- Local commits only; never push.
- No placeholder Fungi and no narrowing of string, binary64, object, Error or
  active-capability behavior.
- No `null`, `NaN`, `else if`, `throw`, `try/catch`, `for` or unbounded `loop`
  in new Fungi.
- Repository closure remains `UNKNOWN`; individual owner success must not be
  aggregated into a substituted release-wide claim.

### Task 1: Bind and classify Slices 168-172

- [x] Classify `InferenceBridge` and `BridgeRegistry` at their erased active
  object and map declarations.
- [x] Classify `assertDeterminism` at its exact `BridgeResult`, string-union and
  JavaScript Error identity boundary.
- [x] Classify `DeterminismMode` and `CertificationProfile` as erased string
  vocabulary declarations with future injective ABI requirements.
- [x] Search existing Fungi assets and all exact consumers before deciding.

### Task 2: Bounded verification

- [x] Run both package typechecks and the complete neutral-contract suite.
- [x] Run focused Tower bridge, attestation and precision consumer tests.
- [x] Run complete Tower-Citizen; require zero failures and zero skips.
- [x] Review both private skills and record a governed disposition for every
  receipt.

### Task 3: Publish the five slices

- [x] Create Slice 168-172 receipts, append the live register and update TODO.
- [x] Run the slice-close owner plus staged path/private-document guards.
- [ ] Commit only the bounded slice documents before running generated owners.

### Task 4: Individual 25-slice maintenance

- [x] Run registered graph generators one at a time and their exact checks;
  do not run `graph-all`.
- [x] Refresh code/dev-tool/Fungi inventories and Myco through their owners.
- [x] Refresh component health, status, roadmap Markdown and subway SVG through
  their registered owners; verify freshness independently.
- [x] Record codebase-memory transport failure as `UNKNOWN`; do not force the
  crash-linked or monolithic re-index lane.
- [x] Commit generated-owner outputs separately and prove a clean tracked tree.

## Self-review

- Erased declarations are accounted without pretending their runtime wire has
  been converted.
- `assertDeterminism` does not become a Boolean convenience projection.
- Every generated output is produced by its owner, not hand-edited.
- K3 `UNKNOWN` and repository-wide `UNKNOWN` never authorize completion.
