# Slices 348-372 photonic target conversion plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `superpowers:subagent-driven-development` to execute this plan task-by-task.
> Workers are read-only; the root agent owns every repository write, review,
> verification and commit.

**Goal:** Account the 25 remaining photonic target symbols without manufacturing
Fungi execution authority from erased TypeScript declarations, hostile host
objects or unproved physical profiles.

**Architecture:** Treat declarations as compile-time-only evidence and examine
the four runtime scopes at their real JavaScript boundary. A slice closes only
with exact source identity, bounded consumer/test evidence, an explicit
threadability result and either a proved Fungi-to-SLIDE/VOK replacement or a
precise fail-closed blocker.

**Tech Stack:** TypeScript 5.9, Node.js 24 `node:test`, Galerina `.fungi`, GIR,
SLIDE/VOK evidence, Myco and the codebase-memory graph.

## Global Constraints

- Pin source HEAD `7db59d867535f7296add76b29266750996855e1b` and
  SHA-256 `0C815A536E0B0F426382373DE537827A622ADEE815834FF7AEFEEBED990A3719`.
- Use codebase-memory first; an unproved indexed build point is navigation only.
- Read both private Fungi skills before adjudication and keep them private and
  unpushed.
- Create no placeholder `.fungi`; checker-only evidence cannot authorize GIR,
  physical `.slide`, re-admission or VOK.
- Preserve `null`/NaN refusal, exhaustive `match`, typed failure and terminal
  `_=>`; do not invent Galerina syntax.
- Run only bounded focused tests and individual owner checks. Do not run the
  crash-linked aggregate tooling, phase-close or monolithic memory lanes.
- Keep adjacent test `.mjs` closure and repository-wide closure `UNKNOWN` until
  their separately governed exits exist.

---

## Scope

| Slice | Exact scope |
|---:|---|
| 348 | `PhotonicTargetStatus` |
| 349 | `PhotonicOperationKind` |
| 350 | `OpticalInterconnectMode` |
| 351 | `OpticalTransferFormat` |
| 352 | `PhotonicTargetCapability` |
| 353 | `OpticalIoCapability` |
| 354 | `PhotonicTargetInput` |
| 355 | `PhotonicLoweringPlan` |
| 356 | `PhotonicOperationMapping` |
| 357 | `UnsupportedPhotonicOperation` |
| 358 | `PhotonicSimulationTarget` |
| 359 | `PhotonicExecutionPlan` |
| 360 | `OpticalIoPlacementRecommendation` |
| 361 | `OpticalIoTransferPlan` |
| 362 | `PhotonicHardwareMappingFile` |
| 363 | `PhotonicFallbackReport` |
| 364 | `OpticalChannelLayout` |
| 365 | `OpticalChannelLayoutReport` |
| 366 | `MatrixOperationMappingReport` |
| 367 | `PhotonicTargetReport` |
| 368 | `PhotonicDiagnostic` |
| 369 | `PHOTONIC_STATUSES` |
| 370 | `photonicDiagnostic` |
| 371 | `validateOpticalChannelLayout` |
| 372 | `validatePhotonicLoweringPlan` |

### Task 1: Establish exact evidence

**Files:**
- Read: `packages-galerina/galerina-target-photonic/src/index.ts`
- Test: `packages-galerina/galerina-target-photonic/tests/photonic-contracts.test.mjs`

- [x] Prove the full source commit, SHA-256 and 304-line boundary.
- [x] Run non-emitting typecheck and the focused test file; require 8/8.
- [x] Reconcile every symbol's consumers through the graph, bounded Myco and
  exact source reads; label stale or unbound index results `UNKNOWN`.
- [x] Search for exact package-owned Fungi, GIR, physical `.slide`, independent
  re-admission and VOK evidence.

### Task 2: Produce independent slice dossiers

**Files:**
- Read: both private Fungi skill authorities and their routed references.
- Produce: read-only worker reports for Slices 348-356, 357-364 and 365-372.

- [x] Record observable semantics, effects, failure identity, aliasing,
  coercion, bounded work and hostile input vectors for every scope.
- [x] Classify erased aliases/interfaces as `NO_RUNTIME_BEHAVIOR`; do not create
  a standalone enum, record or placeholder execution candidate.
- [x] For runtime tables/helpers, require an exact immutable input snapshot,
  closed vocabulary, finite numeric/text/collection bounds, typed diagnostics
  and physical SLIDE/VOK proof before any candidate.
- [x] Record `NO_SKILL_UPDATE` or update each private repository only when a
  genuinely reusable missing rule is proven.

### Task 3: Publish governed receipts and live status

**Files:**
- Create: `docs/reports/slice-348-*-fungi-conversion-2026-08-13.md` through
  `docs/reports/slice-372-*-fungi-conversion-2026-08-13.md`
- Modify: `docs/reports/fungi-conversion-batch-33-42-file-status.md`
- Modify: `docs/TODO.md`
- Modify: `docs/roadmap-2026-07-29-galerina-beta-v1-to-slide.md`

- [x] Give each receipt exactly one full source identity, classification,
  blocker/prerequisite, truthful threadability, both skill dispositions and
  bounded-closure result.
- [x] Preserve confirmed package defects and add their concrete repair exits to
  the TODO rather than translating unsafe host behavior.
- [ ] Run the focused receipt audit before commit and repair the audit test-first
  if the receipt contract exposes a new fail-open gap.
- [ ] Commit authored plan/receipts/status separately from generated owners.

### Task 4: Independent review and bounded owner close

**Files:**
- Modify only registered outputs produced by their individual owner commands.

- [ ] Have independent read-only reviewers compare all receipts, arithmetic,
  pins, classifications, blockers, skill dispositions and physical-authority
  claims to the accepted dossiers.
- [ ] Regenerate retirement, queue, package/project/KB graphs, dev-tool index and
  Fungi inventory individually; commit only exact registered outputs.
- [ ] Regenerate semantic graph, code index, component health, status, SLIDE
  reference, subway/roadmap and Golden outputs individually.
- [ ] Run the individual 19-check owner matrix, then refresh Myco and
  codebase-memory at the final commit and verify exact node/edge counts and
  indexed HEAD.
- [ ] Stop with repository-wide closure `UNKNOWN` unless every separately named
  closure requirement is independently satisfied.
