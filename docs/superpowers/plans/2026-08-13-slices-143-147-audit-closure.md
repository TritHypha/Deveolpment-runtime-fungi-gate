# Slices 143-147 Audit Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Close five remaining audit surfaces and converge the registered
25-slice documentation, graph and index owners without running crash-linked
aggregate lanes.

**Architecture:** Bind exact source behavior to independent package tests,
retain unrepresentable state/effects, publish governed receipts, then execute
bounded owners in dependency order. No focused result substitutes for
repository-wide closure.

**Tech Stack:** TypeScript, Node `node:test`, checked Fungi, SLIDE/VOK, Myco,
registered Node maintenance owners, Markdown receipts.

## Global constraints

- Local commits only; never push.
- Preserve malformed-ledger behavior for parity but record its security debt.
- No placeholder Fungi and no host projection.
- Immutable arrays/records are not active state or effect authority.
- Exclude crash-linked full tooling, normal phase-close, graph-all and
  monolithic memory evaluation.

### Task 1: Verify and classify Slices 143-147

- [ ] Bind `query`, `logTransition`, `getLifecycle`, `TowerAuditEvent` and
  `EgressSink` to source and named tests through the graph/Myco fallback.
- [ ] Confirm the pinned physical profile lacks the complete ledger/effect ABI.
- [ ] Record the silent malformed-row behavior as a separate security TODO.

### Task 2: Run bounded evidence

- [ ] Run Tower-Citizen typecheck.
- [ ] Run the five focused test files and capture exact pass/skip counts.
- [ ] Run complete Tower-Citizen; require zero failures and zero skips.

### Task 3: Review skills and publish receipts

- [ ] Review both private Fungi skills. Update with RED/GREEN/audit/commit only
  for a reusable missing rule; otherwise record `NO_SKILL_UPDATE`.
- [ ] Create five Slice 143-147 reports and update the live register/TODO.
- [ ] Require the slice-close audit to pass **87/87**.
- [ ] Commit bounded documents; do not push.

### Task 4: Converge the 25-slice maintenance owners

- [ ] Run retirement and conversion queue owners/checks.
- [ ] Run semantic, registered project/package/KB graph and code-index owners.
- [ ] Run component-health/status/roadmap/subway owners and freshness checks.
- [ ] Run canonical count and path/private-document checks.
- [ ] Commit only generated owner outputs after fresh checks.
- [ ] Refresh Myco and attempt one moderate codebase-memory refresh. Verify
  indexed head and one new symbol; retain `UNKNOWN` on transport failure.

## Self-review

- Five exact surfaces close the Slices 123-147 window.
- The malformed-ledger concern is visible and is not falsely fixed or ignored.
- Aggregate owner exclusions are explicit; repository closure remains
  `UNKNOWN` until a chunked replacement provides equivalent evidence.
