# Tower authorization Fungi conversion implementation plan

1. Add a failing package test that requires a package-owned authorization
   source and checks the complete K3 table against exported TypeScript.
2. Add `authorization-boundary.fungi` and register it as a loaded asset.
3. Make the interpreter proof green without changing TypeScript.
4. Add and run a physical SLIDE/VOK proof with hostile admission cases.
5. Run the complete Tower-Citizen package and canonical 100-package owner.
6. Refresh retirement and all bounded downstream owners in dependency order.
7. Keep local custody only and preserve repository-wide UNKNOWN while the
   crash-linked closure lanes remain excluded.
