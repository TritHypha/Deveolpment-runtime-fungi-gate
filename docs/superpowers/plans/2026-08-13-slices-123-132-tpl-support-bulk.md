# Slices 123-132 TPL Support and Bulk Operations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close ten TPL support/bulk symbols with exact zero-trust decisions
while retaining every unrepresentable source and caller.

**Architecture:** Bind source behavior to existing independent tests, compare
each contract with the pinned physical SLIDE/VOK surface, create no placeholder
Fungi, and publish governed receipts. Use the live conversion register as the
working tab; defer aggregate owners to the Slice 147 boundary.

**Tech Stack:** TypeScript, Node `node:test`, checked Fungi, SLIDE/VOK, Myco,
Markdown receipts.

## Global Constraints

- Local branch and commits only; never push.
- Pinned SLIDE is `ed326eaa14f1a899841cbac8da353d400970367e`.
- Preserve Error identity, arithmetic-Trit brand, numeric domain, typed memory,
  mutation, cleanup ordering, active capabilities and audit effects.
- No placeholder Fungi; no host projection.
- Do not run roadmap/subway/graph/index aggregate owners before Slice 147.
- Do not run crash-linked full tooling, phase-close, graph-all or monolithic
  memory evaluation.

---

### Task 1: Verify source and profile evidence

**Files:**
- Read: `packages-galerina/galerina-tower-citizen/src/tpl-simulator.ts`
- Read: `../SLIDE/src/safe-value-envelope.mjs`
- Read: `../SLIDE/src/checked-fungi-pure-scalar-compiler.mjs`

**Interfaces:**
- Consumes: exact ten source contracts and pinned physical type table.
- Produces: the design table classifications.

- [ ] Use Myco to bind the error classes, TritState, T-MAC, load and erase
  behaviors to named package tests.
- [ ] Verify the sibling SLIDE commit and absence of Error identity, arithmetic
  Trit, Float, mutable typed memory and active capability types.
- [ ] Record each exact blocker; create no `.fungi` asset.

### Task 2: Run bounded verification

**Files:**
- Test: `packages-galerina/galerina-tower-citizen/tests/ternary-ops.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/governance-algebra-binding.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/tpl-simulator.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/tpl-bitnet-fidelity.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/bridge.test.mjs`

**Interfaces:**
- Consumes: unchanged source and independent test oracles.
- Produces: exact focused/package pass and skip counts.

- [ ] Run `npm run typecheck`, then the five named files with `node --test`.
- [ ] Run complete package `npm test` with a bounded captured summary.
- [ ] Stop on any failure or skip; do not publish receipts from partial proof.

### Task 3: Review skills and publish the working tab

**Files:**
- Review: `../skills/translating-typescript-to-fungi/`
- Review: `../skills/writing-fungi/`
- Create: ten `docs/reports/slice-123-...` through `slice-132-...` reports.
- Modify: `docs/reports/fungi-conversion-batch-33-42-file-status.md`
- Modify: `docs/TODO.md`

**Interfaces:**
- Consumes: exact evidence from Tasks 1-2.
- Produces: ten governed receipts and a current operational tab.

- [ ] Check whether Error identity and cleanup rules are already explicit. If
  a reusable gap exists, use the writing-skills RED/GREEN/audit/commit cycle;
  otherwise record `NO_SKILL_UPDATE`.
- [ ] Write ten reports with exact blocker, evidence, reopen condition,
  threadability and non-authority consequence.
- [ ] Update the live register and TODO through Slice 132; deliberately leave
  roadmap/subway/graphs/index for the Slice 147 batch boundary.
- [ ] Run `node scripts/audit-conversion-slice-close.mjs`; require **72/72**.
- [ ] Commit only the design, plan, reports and working-tab documents; leave a
  clean worktree and do not push.

## Self-review

- All ten symbols and their distinct failure/state/effect dimensions are mapped.
- No physical or retirement claim is inferred from frontend or package tests.
- The 25-slice owner cadence is explicit, bounded and does not omit per-slice
  receipts or focused verification.
