# Resolve Host Fungi Conversion Implementation Plan

**Goal:** Physically execute an exact fail-closed Fungi twin for TypeScript
`resolveHost` without changing or retiring its registry or consumers.

## Constraints

- Admit only the three exact declared host names.
- Map TypeScript absence to the explicit `"<undeclared>"` adapter String.
- Preserve every external record field, order, Boolean, and custody String.
- Treat profiles as non-authorizing data; retain `authorityReleased: false`.
- Add no null, NaN, `else if`, `else`, throw, try/catch, `for`, `while`, or
  `loop`.
- Keep TypeScript and all consumers active; commit locally and never push.
- Exclude full tooling, normal phase-close, and monolithic memory evaluation.

## Tasks

1. Add a RED differential test for declared, absent, unknown, and hostile names
   plus prohibited source shapes.
2. Add the closed host-capability record and minimal `resolveHostFungi` flow to
   the existing hardening trust module; make the focused test green.
3. Add a physical SLIDE/VOK record proof with ABI, work, and mutation refusals.
4. Run the bounded package, canonical, Golden, retirement, graph, roadmap, and
   index owners; update TODO, roadmap, SVG, and a custody report.
