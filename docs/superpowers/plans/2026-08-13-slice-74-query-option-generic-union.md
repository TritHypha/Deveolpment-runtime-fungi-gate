# Slice 74 Query Option Generic Union Plan

**Goal:** Preserve the complete exported generic tagged-union contract without
specializing its payload or projecting variant authority into the host.

- [x] Locate the exact type, helper and caller graph through codebase memory.
- [x] Verify no duplicate adjudication, replacement, package asset or floor.
- [x] Record the generic source type-predicate and runtime property behavior.
- [x] Compare frontend `Option<T>` with the pinned physical SLIDE type table.
- [x] Reject concrete `Option<Int>`, tag-only and precomputed-Boolean mappings.
- [x] Run the complete owning-package typecheck, build and test lane.
- [x] Update the live register, TODO and Slice 74 report.
- [x] Review both private skills and run bounded queue, receipt, path and
  private-document checks before committing locally without pushing.
