# Slices 99-102 tensor, consensus and confidence plan

1. Bind the exact exported TypeScript source and existing focused tests for
   `vAndTensor`, `vAndTensor2D`, `consensusTritN` and `collapseConfidence`.
2. Reuse the current complete Tower-Citizen package lane as source-contract
   evidence; do not create a narrowed Fungi probe and relabel refusal as parity.
3. Publish four canonical blocker reports with exact typed-array, array,
   accumulator and binary64 reopen conditions.
4. Update the live register, TODO, assurance status and active roadmap.
5. Run the governed slice-receipt audit and the bounded affected owner matrix;
   keep all crash-linked aggregate lanes excluded.
6. Review both private Fungi skills, record `NO_SKILL_UPDATE` unless a reusable
   gap is proved, commit locally and keep all TypeScript consumers active.
