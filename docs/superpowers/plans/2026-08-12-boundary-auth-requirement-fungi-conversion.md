# Boundary Authentication Requirement Fungi Conversion Implementation Plan

## Disposition

`CANCELLED — BLOCKED_BY_BOOTSTRAP_FLOOR`

The conversion queue refused the proposed symbol because
`boundary-graph.ts` is a declared `bounded-bootstrap-floor` file. That refusal
is the intended zero-trust result. None of the implementation tasks below are
authorized, and they must not be used as a work queue unless a later binding
governance decision first removes the floor dependency.

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `superpowers:subagent-driven-development` or `superpowers:executing-plans` to
> implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Prove the graph-algorithms package's `requiresAuth` decision as an
exact package-owned `.fungi` twin through canonical execution and physical
SLIDE/VOK, without switching or retiring TypeScript.

**Architecture:** Admit one exact symbol in the conserved queue. Preserve its
closed six-String source union as an exhaustive String match, compare every
source state, then compile the exact bytes into one physical `.slide` and
re-admit it through VOK.

**Tech stack:** Galerina `.fungi`, TypeScript/JavaScript, `node:test`, canonical
GIR/WAT/Wasm proof, independent SLIDE package compiler and VOK receipt verifier.

## Global constraints

- Authorize only `requiresAuth`, not `boundary-graph.ts`.
- Preserve true for `api`, `webhook`, `secure`; false for `internal`, `package`,
  `public`, and every surplus admitted String.
- Use exhaustive `match` with terminal `_ =>`.
- Use no null, NaN, `else if`, exception syntax, `for`, `while` or `loop`.
- Add no effect, capability, contract permission, Hallmark, border grant,
  global vault access or host API.
- Keep TypeScript and `buildBoundaryGraph` active.
- Commit locally only; do not push.
- Do not run full tooling, `graph-all`, normal phase-close or monolithic memory
  evaluation.

---

### Task 1: Exact queue admission — cancelled

**Files:**
- Modify: `governance/conversion-queue-decisions.json`
- Generated: `build/conversion-queue/queue.json`
- Generated: `build/conversion-queue/QUEUE.md`

**Produces:** one `SYMBOLS`-scoped candidate for
`boundary-graph.ts#requiresAuth`, bound to this committed design digest.

- [x] Commit the design before requesting candidate authority.
- [x] Attempt the exact symbol-scoped decision.
- [x] Observe the queue's fail-closed bootstrap-floor refusal.
- [x] Restore the last completed queue decision; grant no candidate authority.

### Task 2: Differential RED/GREEN — not authorized

**Files:**
- Create: `packages-galerina/galerina-devtools-graph-algorithms/tests/boundary-auth-requirement-fungi-conversion.test.mjs`
- Create: `packages-galerina/galerina-devtools-graph-algorithms/src/self-hosted/boundary-auth-requirement.fungi`
- Modify: `packages-galerina/galerina-devtools-graph-algorithms/package.json`

**Produces:** `pure flow requiresAuth(callerKind: String) -> Bool` plus a
complete six-value and hostile-surplus differential proof.

- [ ] Write a failing test that requires the absent source and loaded asset.
- [ ] Run it and verify the failure is only the missing Fungi asset.
- [ ] Add the minimal Fungi match and loaded-asset declaration.
- [ ] Strict-check the exact source.
- [ ] Prove the live TypeScript caller surface, interpreter and signed Wasm for
  all six union members plus empty, case-changed, padded, NUL and unknown text.
- [ ] Run the owning package and commit.

### Task 3: Physical SLIDE/VOK proof — not authorized

**Files:**
- Create: `scripts/tests/boundary-auth-requirement-fungi-slide.integration.test.mjs`
- Modify: `governance/phase-close-commands.json`

**Produces:** one physical `.slide`, independently re-admitted through VOK with
typed Boolean receipts and fail-closed mutation evidence.

- [ ] Write the physical test using the exact package-owned source bytes.
- [ ] Verify all six source variants and hostile surplus Strings.
- [ ] Refuse missing, surplus, non-String, invalid UTF-16 and exhausted-work
  arguments.
- [ ] Refuse source, receipt, safe-value and physical artifact mutations.
- [ ] Register the focused physical test, run with zero skips, and commit.

### Task 4: Governed closure — replaced by refusal record

**Files:**
- Create: `docs/reports/boundary-auth-requirement-fungi-conversion-2026-08-12.md`
- Modify: `docs/TODO.md`
- Modify: `docs/ROADMAP.md`
- Generated: bounded package, project, KB, inventory, semantic, code-index,
  count, status, percent and roadmap/subway owners.

**Produces:** a local, reference-only Slice 33 receipt with current bounded
owners and fresh indexes.

- [ ] Review both public Fungi skills; update with reusable evidence or record
  `NO_SKILL_UPDATE`.
- [ ] Update TODO, active roadmap and the dated report.
- [ ] Run only candidate, package, physical and bounded owner checks.
- [ ] Refresh the codebase graph and Myco at the final commit and verify the
  source symbol, Fungi flow and scoped queue are queryable.
- [ ] Verify clean tracked worktrees and local commits; do not push.
