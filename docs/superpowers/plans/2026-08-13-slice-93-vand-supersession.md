# Slice 93 vAnd supersession plan

1. Add a focused Tower-Citizen test that binds the exact `vAnd` export and
   refuses a duplicate `verdict-and.fungi` asset.
2. Use nine literal K3-minimum rows to compare the TypeScript export with the
   existing checked `effectiveVerdict` Fungi flow.
3. Rerun the focused supersession test, the existing Slice 91 physical
   SLIDE/VOK proof with the pinned sibling SLIDE path, and the complete affected
   package.
4. Publish a governed Slice 93 report with
   `SUPERSEDED_BY_EXISTING_FUNGI`, `PARALLEL_PURE`, and bounded closure
   `COMPLETE`.
5. Update the live register, TODO and roadmap; regenerate only registered
   dependent owners in dependency order.
6. Review both private Fungi skills and record either a verified commit or
   `NO_SKILL_UPDATE`.
7. Keep crash-linked aggregate lanes excluded and retain repository-wide
   closure as `UNKNOWN`.
