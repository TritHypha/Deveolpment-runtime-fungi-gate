# Implicit return-type Fungi conversion implementation plan

1. Add a failing differential test that requires a package-owned Fungi asset
   and exact public-caller parity.
2. Add the bounded `.fungi` decision with no forbidden control or value forms,
   and declare it in the naming package graph.
3. Build and test `@galerina/devtools-naming` plus the focused conversion test.
4. Add a root integration test that compiles, publishes, re-admits, executes,
   and receipt-verifies the exact source through SLIDE/VOK.
5. Prove hostile argument, source, and artifact mutation refusal.
6. Record the reference-only evidence, update the conversion ledger, TODO and
   roadmap through their registered owners, and keep broader closure UNKNOWN.
7. Commit locally, refresh both repository indexes, and verify the committed
   build points and new symbols without pushing.
