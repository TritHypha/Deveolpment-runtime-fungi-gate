# Generic type-name Fungi conversion implementation plan

1. Add a failing package test that requires the governed source asset and
   checks exact parity with the public naming decision.
2. Add `generic-type-name.fungi` and register it as a package-loaded asset.
3. Make the differential proof green without changing the TypeScript decider.
4. Add a physical SLIDE/VOK integration proof using the existing immutable
   text-trim registry and its exact descriptor digest.
5. Run the complete naming package, then the complete package owner.
6. Refresh retirement, semantic, graph, percentage, roadmap, count, Golden,
   code-index, Myco, and primary graph owners in dependency order.
7. Commit only explicit paths, keep both repositories local-only, and retain
   repository-wide closure as UNKNOWN because crash-linked tooling,
   phase-close, graph-all-after-roadmap, and whole-memory evaluation remain
   excluded.
