# Slices 218-222 Text Rendering and Maintenance Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:subagent-driven-development (recommended) or
> superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** Account for the final metrics text helpers and first logger
declarations, then close the scheduled Slice 222 graph/index/roadmap boundary.

**Architecture:** Preserve JavaScript UTF-16 ordering, regex replacement,
numeric stringification and exact Prometheus wire bytes in TypeScript until one
physical profile admits the complete graph. Treat logger type declarations as
non-executing descriptions, publish bounded receipts, then refresh registered
owners individually without invoking crash-linked aggregates.

**Tech Stack:** TypeScript, Node `node:test`, Myco, checked Fungi/SLIDE evidence,
private Fungi skills and governed owner scripts.

## Global Constraints

- Local commits only; never push.
- No placeholder Fungi or host-rendered authoritative text.
- New Fungi may not contain `null`, `NaN`, `else if`, `throw`, `try/catch`,
  `for` or `loop`; iteration requires a proved bounded Boolean `while`.
- Preserve exact UTF-16 comparison, regex, escaping, numeric rendering, record
  traversal, series filtering and newline bytes.
- Run owners individually at Slice 222; do not run full tooling, normal
  phase-close, `graph-all` or monolithic memory evaluation.
- Repository-wide closure remains `UNKNOWN`.

---

### Task 1: Bind Slices 218-222

**Files:**
- Read: `packages-galerina/galerina-observability/src/metrics.ts`
- Read: `packages-galerina/galerina-observability/src/logger.ts`
- Test: `packages-galerina/galerina-observability/tests/metrics.test.mjs`
- Test: `packages-galerina/galerina-observability/tests/logger.test.mjs`
- Test: `packages-galerina/galerina-observability/tests/kernel-integration.test.mjs`

**Interfaces:**
- Consumes: `cmp`, `promLabel`, `renderMetricsPrometheus`, `LogLevel` and
  `LogRecord` at pinned source digests.
- Produces: three exact physical blockers and two
  `NO_RUNTIME_BEHAVIOR` declarations.

- [x] Retain `cmp` at JavaScript same-type UTF-16 string ordering and exact
  `-1/0/+1` return semantics.
- [x] Retain `promLabel` at the global JavaScript regex and UTF-16 replacement
  boundary.
- [x] Retain `renderMetricsPrometheus` at complete record/array traversal,
  label validation/escaping, conditional series, number-to-text and exact wire
  layout.
- [x] Account `LogLevel` and `LogRecord` without treating erased declarations
  as validation, redaction, clock provenance or sink authority.

### Task 2: Verify and publish the five receipts

**Files:**
- Create: `docs/reports/slice-218-string-cmp-fungi-conversion-2026-08-13.md`
- Create: `docs/reports/slice-219-prom-label-fungi-conversion-2026-08-13.md`
- Create: `docs/reports/slice-220-render-metrics-prometheus-fungi-conversion-2026-08-13.md`
- Create: `docs/reports/slice-221-log-level-fungi-conversion-2026-08-13.md`
- Create: `docs/reports/slice-222-log-record-fungi-conversion-2026-08-13.md`
- Modify: `docs/reports/fungi-conversion-batch-33-42-file-status.md`
- Modify: `docs/TODO.md`

**Interfaces:**
- Consumes: five classifications and fresh bounded verification.
- Produces: five governed receipts and the Slice 222 checkpoint.

- [x] Run complete observability **36/36** and focused metrics/logger/kernel
  **27/27**, with zero failures and zero skips.
- [x] Write the five receipts and update the live register/TODO.
- [x] Review both private skills and record an evidence-based update or
  `NO_SKILL_UPDATE`.
- [x] Run receipt, staged path-leak and private-document guards and commit only
  bounded authored files.

### Task 3: Close the Slice 222 maintenance boundary

**Files:**
- Modify: registered generated conversion, graph, inventory, index,
  component-health, status, roadmap/subway, count and receipt outputs through
  their owning tools only.

**Interfaces:**
- Consumes: the committed Slice 218-222 authored record.
- Produces: current bounded owner outputs and explicit `UNKNOWN` for unavailable
  exact codebase-memory freshness.

- [x] Run retirement and conversion-queue owners individually.
- [x] Run package, project, KB and semantic graph owners individually; do not
  invoke `graph-all`.
- [x] Run dev-tool/Fungi inventories, code-index, component-health, status,
  roadmap/subway, canonical counts, receipts, Golden and leak guards.
- [ ] Commit registered owner outputs and rerun exact freshness checks.
- [ ] Refresh Myco once after the final commit and prove Slice 222 queryable.
- [x] Record codebase-memory as `UNKNOWN` while its transport is unavailable.

## Self-review

- Frontend String comparison does not prove JavaScript UTF-16 ordering on the
  selected physical SLIDE/VOK profile.
- Exact Prometheus text is a wire contract, not merely equivalent prose.
- Erased logger declarations grant no redaction or output authority.
- K3 and generated percentages remain evidence only; they do not authorize
  conversion or release.
