# Slices 298-322 CPU, GPU and JS target conversion plan

## Goal

Account the remaining CPU target symbols, the complete GPU target surface and
the first eight JS target declarations without manufacturing Fungi execution
authority from erased TypeScript shapes or planning-only validators.

## Scope

| Slice | Exact scope |
|---:|---|
| 298 | `CpuTargetDiagnosticSeverity` |
| 299 | `CpuTargetDiagnostic` |
| 300 | `CpuCalibrationSample` |
| 301 | `CpuCalibrationReport` |
| 302 | `supportsCpuFeatures` |
| 303 | `validateCpuFeatureProbe` |
| 304 | `selectCpuTargetPlan` |
| 305 | `GpuTargetCapability` |
| 306 | `GpuKernelPlan` |
| 307 | `GpuTargetReport` |
| 308 | `GpuDiagnosticSeverity` |
| 309 | `GpuDiagnostic` |
| 310 | `GPU_BACKENDS` |
| 311 | `gpuDiagnostic` |
| 312 | `validateGpuKernelPlan` |
| 313 | `createGpuTargetReport` |
| 314 | `JsRuntime` |
| 315 | `JsModuleFormat` |
| 316 | `SourceMapMode` |
| 317 | `SourceMapRule` |
| 318 | `JsOutputPlan` |
| 319 | `EsModuleMetadata` |
| 320 | `FrameworkAdapterMetadata` |
| 321 | `JsBundleCheckOutcome` |
| 322 | `JsBundleReport` |

## Evidence and close rules

1. Pin the full Git commit and SHA-256 of each source file. Try codebase-memory
   first; if its transport/build point is unproved, mark it `UNKNOWN` and use
   current Myco plus exact source/test reads as bounded fallback.
2. Run focused package tests and inspect exact consumers and package-owned
   Fungi/GIR/physical `.slide`/re-admission/VOK evidence.
3. Classify erased aliases/interfaces as `NO_RUNTIME_BEHAVIOR`; do not create
   placeholder types. Executable validators remain blocked until hostile host
   records, text/array semantics, bounded work and physical profiles are proved.
4. Treat diagnostic/report Booleans and planning metadata as transport, not
   authority. Require one exact immutable evidence snapshot for any derived
   result and refuse absent or surplus evidence under zero trust.
5. Review both private skills at every slice. Update only for a reusable rule
   not already present; keep all changes private and unpushed.
6. At Slice 322 update the live register, TODO, roadmap and subway, then run the
   individual owner matrix. Do not run crash-linked aggregate lanes; repository
   closure remains `UNKNOWN`.

`canUseLowBitCpuPath` is not recounted: Slice 50 already governs that exact
symbol. The current wave may add evidence to its blocker, but it receives zero
new queue-credit and no duplicate receipt.

## Focused verification

- CPU package tests: current baseline 3/3.
- GPU package tests: current baseline 5/5.
- JS package tests: current baseline 13/13.
- Receipt audit, exact owner checks, graph integrity, canonical counts, Golden
  evidence and leak audits at the maintenance boundary.
