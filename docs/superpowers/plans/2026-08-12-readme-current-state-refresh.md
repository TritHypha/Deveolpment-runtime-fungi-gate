# Galerina README current-state refresh plan

## Goal

Rewrite `README.md` as the current public entry point without widening authority or duplicating detailed ledgers.

## Tasks

1. Inventory every existing README section and classify each claim as current, stale, generated or unsupported.
2. Verify current package/test counts, execution lanes, CLI commands, package families, Golden Pack syntax and benchmark evidence from their owners.
3. Replace the README with the evidence-led structure defined in the companion design.
4. Preserve the generator-owned subway markers and generated content.
5. Run focused README, link, path-leak, canonical-count, roadmap and package-graph checks.
6. Review the final diff for unsupported production, performance or security claims.
7. Commit locally, refresh the codebase index and verify the indexed build point.

## Verification boundary

This documentation task does not authorize the known crash-linked repository-wide full-tooling, normal phase-close, whole-memory evaluation or post-roadmap graph-all lanes. Their status remains unknown unless separately re-established.
