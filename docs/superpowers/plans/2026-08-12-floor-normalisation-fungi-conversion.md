# Governance floor normalisation Fungi conversion implementation plan

1. Prove and repair prototype-property escape in the TypeScript alias lookup.
2. Add a failing differential test requiring a package-owned Fungi asset and
   the complete repaired floor table.
3. Add the exhaustive String `match` flow and register it as a loaded asset.
4. Prove exact public governance-verifier obligations for every accepted alias.
5. Run the focused differential proof and complete compiler package.
6. Add and run an independent physical SLIDE/VOK proof with hostile inputs.
7. Run the canonical 100-package owner, then refresh bounded downstream owners
   in dependency order.
8. Keep local custody only; do not infer consumer-switch, release, production,
   or retirement authority.
