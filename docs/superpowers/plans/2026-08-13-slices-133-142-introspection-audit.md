# Slices 133-142 Introspection and Audit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Close ten introspection/audit symbols with exact zero-trust decisions
while retaining every unrepresentable source and caller.

**Architecture:** Bind each source contract to existing independent tests,
compare it with the pinned physical SLIDE/VOK surface, create no placeholder
Fungi, and publish governed receipts. Immutable array/record transport is not
active-object or effect authority.

**Tech Stack:** TypeScript, Node `node:test`, checked Fungi, SLIDE/VOK, Myco,
Markdown receipts.

## Global constraints

- Local branch and commits only; never push.
- Pinned SLIDE is `ed326eaa14f1a899841cbac8da353d400970367e`.
- Preserve object state, clocks, callbacks, record shape/spread, JSON, egress,
  filesystem durability and mutation/effect ordering.
- No placeholder Fungi and no host projection.
- Do not run roadmap/subway/graph/index aggregate owners before Slice 147.
- Do not run crash-linked full tooling, phase-close, graph-all or monolithic
  memory evaluation.

---

### Task 1: Verify source and physical-profile evidence

**Files:**
- Read: `packages-galerina/galerina-tower-citizen/src/tpl-simulator.ts`
- Read: `packages-galerina/galerina-tower-citizen/src/audit-logger.ts`
- Read: `../SLIDE/src/safe-value-envelope.mjs`
- Read: `../SLIDE/src/checked-fungi-pure-scalar-compiler.mjs`

- [ ] Bind all ten contracts to named package tests through Myco.
- [ ] Verify the sibling SLIDE commit and distinguish immutable array/record
  transport from absent mutable-object, clock, callback and effect authority.
- [ ] Record each exact blocker and create no `.fungi` asset.

### Task 2: Run bounded verification

**Files:**
- Test: `packages-galerina/galerina-tower-citizen/tests/tpl-simulator.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/tpl-bitnet-fidelity.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/tower-citizen.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/sentinel-egress-time.test.mjs`
- Test: `packages-galerina/galerina-tower-citizen/tests/flight-boot.test.mjs`

- [ ] Run package typecheck, then the five named files with `node --test`.
- [ ] Run the complete package test lane and capture exact pass/skip counts.
- [ ] Stop on failure or skip; never publish receipts from partial proof.

### Task 3: Review skills and publish the working tab

**Files:**
- Review: `../skills/translating-typescript-to-fungi/`
- Review: `../skills/writing-fungi/`
- Create: ten `docs/reports/slice-133-...` through `slice-142-...` reports.
- Modify: `docs/reports/fungi-conversion-batch-33-42-file-status.md`
- Modify: `docs/TODO.md`

- [ ] Check whether immutable transport versus active-object/effect authority is
  explicit in both private skills. Use the writing-skills RED/GREEN/audit/commit
  cycle only for a reusable gap; otherwise record `NO_SKILL_UPDATE`.
- [ ] Write ten reports with exact blocker, evidence, reopen condition,
  threadability and non-authority consequence.
- [ ] Update the live register/TODO through Slice 142; leave aggregate owners
  for Slice 147.
- [ ] Require `node scripts/audit-conversion-slice-close.mjs` to pass **82/82**.
- [ ] Commit only the design, plan, reports and working-tab documents; do not
  push.

## Self-review

- The physical profile's immutable arrays/records are acknowledged and not
  overclaimed as support for active state or effects.
- Exact clock, sequence, JSON, spread, egress, durability and cleanup ordering
  remain source-owned.
- Per-slice proof continues despite the deferred 25-slice aggregate cadence.
