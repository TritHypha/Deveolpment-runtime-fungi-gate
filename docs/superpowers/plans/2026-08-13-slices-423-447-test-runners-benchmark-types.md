# Slices 423-447 test runners and benchmark types plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `superpowers:subagent-driven-development`. Workers are read-only; the root
> agent owns repository writes, verification and commits.

**Goal:** Complete the second 25-slice wave of the 50-slice maintenance cadence
by accounting the remaining `galerina-test` runner scopes and the first 14
`galerina-tools-benchmark` declarations.

**Architecture:** Keep test process orchestration separate from erased
benchmark transport shapes. Treat adjacent MJS tests as focused evidence, not
silent conversion credit, and preserve every host effect, alias and physical
profile gap as a named refusal.

**Tech Stack:** TypeScript 5.9, Node.js 24, `node:test`, Galerina `.fungi`, GIR,
SLIDE/VOK, Myco and codebase-memory.

## Global Constraints

- Pin source build point `e92a8e4aa0b48331875f78084ffc3d3c284862e0` and
  each full source SHA-256.
- Use codebase-memory first and bounded Myco `--no-refresh` only as fallback.
- Read both private Fungi skills; keep them private and unpushed.
- Never author null, NaN, `else if`, throw/try/catch, `for` or `loop` in Fungi.
- Use only bounded Boolean `while`; conserve cancellation, timeout, failure,
  output, process and authority ordering.
- Do not create authorizing/retiring candidates without exact GIR, physical
  `.slide`, independent re-admission and VOK evidence for all types/effects.
- Do not run crash-linked aggregate tooling, phase-close or monolithic memory.
- Repository-wide and adjacent test-MJS closure remain `UNKNOWN`.

---

## Scope

| Slice | Exact scope |
|---:|---|
| 423 | `DEFAULT_E2E_EXAMPLES` |
| 424 | `targetMissing` |
| 425 | `runUnit` |
| 426 | `runE2e` |
| 427 | `runConformance` |
| 428 | `runFidelity` |
| 429 | `discoverExactTests` |
| 430 | `runExactNodeCorpus` |
| 431 | nested `onOutput` |
| 432 | `runSlide` |
| 433 | `runAll` |
| 434 | `BenchmarkMode` |
| 435 | `BenchmarkTrigger` |
| 436 | `BenchmarkTarget` |
| 437 | `BenchmarkStatus` |
| 438 | `BenchmarkPrivacyPolicy` |
| 439 | `BenchmarkConfig` |
| 440 | `BenchmarkSystemInfo` |
| 441 | `BenchmarkTestResult` |
| 442 | `BenchmarkScores` |
| 443 | `BenchmarkReport` |
| 444 | `BenchmarkSubmitPayload` |
| 445 | `DEFAULT_BENCHMARK_CONFIG` |
| 446 | `BenchmarkDiagnosticSeverity` |
| 447 | `BenchmarkDiagnostic` |

### Task 1: Pin sources and evidence

- [x] Pin `runners.ts` SHA-256
  `A9F7395D08F1DDC97087B8E8BBF163EE10C3C72684B4FB889BB0F456B13FD645`.
- [x] Pin benchmark `index.ts` SHA-256
  `5C472FBC5723A5D50D824542C7258092A9048109D1800EBD9895333BBA1F932A`.
- [x] Establish focused typecheck/tests and distinguish existing-dist evidence.
- [x] Reconcile prior receipts, callers, assets and physical evidence.

### Task 2: Produce independent dossiers

- [x] Record runner ordering, boundedness, child processes, filesystem, clocks,
  callbacks, aggregation, bail behavior, partial results and failure routes.
- [x] Record benchmark erasure, exact record/array/Option/number/text domains,
  aliasing, literal fields and any runtime constant identity.
- [x] Give every scope an exact classification, threadability, blocker/exit,
  hostile vectors and evidence-based skill disposition.

### Task 3: Publish and review the 50-slice boundary

- [x] Create 25 receipts and update register, TODO and roadmap.
- [x] Run focused checks and receipt audit; obtain three read-only reviews.
- [x] Commit authored evidence separately from generated owners.
- [x] Regenerate all registered owners individually for Slices 398-447.
- [x] Run the 19-check bounded owner matrix.
- [ ] Commit the final closure/provenance documents, then refresh Myco and
  codebase-memory at the resulting exact committed HEAD and independently
  prove both indexed build points.
- [x] Keep repository closure `UNKNOWN`; never substitute excluded aggregates.
