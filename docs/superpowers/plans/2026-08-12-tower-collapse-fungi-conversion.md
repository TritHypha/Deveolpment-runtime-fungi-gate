# Tower collapse Fungi conversion implementation plan

1. Add a failing package test that requires `collapseVerdict` and compares it
   with exported TypeScript `collapse` over all three K3 values.
2. Extend `authorization-boundary.fungi` with the exhaustive typed flow.
3. Make the differential proof green without changing TypeScript.
4. Add and run an independent physical SLIDE/VOK proof with hostile admission
   cases and typed String receipts.
5. Run the complete Tower-Citizen package and canonical 100-package owner.
6. Refresh retirement and all bounded downstream owners in dependency order.
7. Keep local custody only and preserve repository-wide UNKNOWN while the
   crash-linked closure lanes remain excluded.
