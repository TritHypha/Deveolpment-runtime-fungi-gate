# Package scalar quote stripping Fungi conversion implementation plan

1. Add a failing package-owned differential test over balanced, unbalanced,
   mixed, whitespace, hostile and Unicode scalar values.
2. Add the pure Fungi trim-and-balanced-quote flow and register it as a loaded
   compiler asset.
3. Prove typed Fungi parity with the real `loadPackageManifest` caller.
4. Run the complete package-resolver neighborhood and compiler package owner.
5. Record the existing-registry refusal, add SLIDE Contract 84 as a separately
   tested immutable-text-slice successor, and prove physical SLIDE/VOK against
   its exact registry identity without widening any predecessor.
6. Run the canonical package owner and bounded generated owners in dependency
   order after the next batching decision.
7. Keep local custody only; do not infer consumer-switch, release, production
   or retirement authority.
