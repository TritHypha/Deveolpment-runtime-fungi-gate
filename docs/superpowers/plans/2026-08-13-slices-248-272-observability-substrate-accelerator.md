# Slices 248-272 observability, substrate and accelerator plan

## Goal

Account the next 25 TypeScript scopes without inventing Fungi syntax, narrowing
JavaScript behavior, bypassing a bootstrap floor, or promoting checker evidence
to physical SLIDE/VOK authority.

## Bound scope

- Slices 248-257: remaining observability route and composition symbols.
- Slices 258-266: substrate-math declarations and executable calculus.
- Slices 267-272: accelerator string-literal declarations.

Root is the sole writer and admission owner. Three read-only dossiers may locate
callers, tests, current physical evidence and hostile vectors. Every material
finding must be independently reproduced before it enters the ledger.

## Acceptance gates

1. Pin the live source commit and SHA-256 for every source file.
2. Reconcile package boundaries, retirement state, conversion queue and exact
   Fungi/SLIDE/VOK assets before authoring.
3. Classify each scope as `CANDIDATE`, `BLOCKED`, `NO_RUNTIME_BEHAVIOR`,
   `SUPERSEDED_BY_EXISTING_FUNGI`, or `BOOTSTRAP_FLOOR`.
4. Create no placeholder Fungi. A positive candidate requires strict compiler,
   physical `.slide`, independent re-admission, VOK and hostile-vector proof.
5. Review both private skills at every slice close; update and verify reusable
   rules or record a reasoned `NO_SKILL_UPDATE`.
6. At Slice 272, update the live register, TODO, roadmap and subway; run only
   individually registered safe owners and focused tests. Crash-linked aggregate
   lanes remain excluded, so repository-wide closure remains `UNKNOWN`.

## Result

All 25 scopes are accounted. No positive candidate exists at the current
boundary. Nine declarations are `NO_RUNTIME_BEHAVIOR`; the substrate interface
is the tenth. The remaining active scopes are blocked by exact active-capability,
open-record, async, text, Float, Error, bounded-work or physical-profile gaps;
the substrate executable file also remains an explicit bounded bootstrap floor.

Two reusable lessons were added to both private skills: erased TypeScript shape
restrictions cannot protect trusted fields from a later untrusted spread, and
host fields must be captured once from inert own data before validation and use.
Both skill repositories remain private and unpushed.

The Slice 272 individual owner matrix is green at maintenance commit
`b9f2edb9`: 1,486/1,486 queue paths, 100 packages / 201 outputs, KB 4/4,
project 5/5, graph integrity 9,716 nodes / 9,900 edges / zero violations,
semantic 3/3 with 974 tests, code index 974, canonical counts 7/7 at 9,612,
212/212 receipts, Golden 11/11 and roadmap 5/5. No crash-linked aggregate was
run; repository-wide closure remains `UNKNOWN`.
