# Slice 63 bootstrap-floor adjudication

## Candidate

`packages-galerina/galerina-core-security/src/index.ts#isHighRiskPermissionAction`
is a six-label, case-sensitive String membership decision that otherwise fits
the reconciled physical scalar profile.

## Ruling

`BLOCKED_BY_BOOTSTRAP_FLOOR`.

The retirement ledger classifies `@galerina/core-security` as
`T1-trust-root` with declared floor `bounded-bootstrap-floor`. The conversion
queue refuses any decision override for that path. A reference twin can pass
local semantic and physical probes yet still has no authority to bypass the
source-to-SLIDE bootstrap fixpoint. The exploratory asset and test were removed
after the queue refusal; no candidate decision, Fungi asset or retirement claim
is retained.

The function may be reconsidered only when the bootstrap-floor owner provides
fixpoint or platform evidence and the retirement graph changes through its
governed owner. Until then, the TypeScript source remains authoritative.
