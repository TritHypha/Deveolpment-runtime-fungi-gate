# Follow-on Fungi Dossier Adjudication

## Decision

The product-owner gate admits no implementation candidate from the follow-on
dossier wave. Slices 38, 39, 40, 41, the replacement Slice 42 and Slice 43 are
all bound as `BLOCKED`. The original Slice 42 proposal remains
`SUPERSEDED_BY_EXISTING_FUNGI` by Slice 29 and is not counted twice.

This is a negative design record. It authorizes documentation and queue
classification only. It does not authorize a Fungi asset, profile widening,
consumer switch, TypeScript deletion, retirement, production, release or push.

## Bound outcomes

| Slice | Exact symbol | Owner decision | Decisive boundary |
|---:|---|---|---|
| 38 | `galerina-web/src/index.ts#isServerOnlyImport` | `BLOCKED` | The 28-label decision exceeds the proved seven-arm physical String-match ceiling. |
| 39 | `galerina-target-js/src/index.ts#isServerOnlyImport` | `BLOCKED` | The same decision exceeds the physical control ceiling and the physical text boundary narrows the source String domain. |
| 40 | `galerina-devtools-provenance/src/analyzer.ts#isGateCall` | `BLOCKED` | Its only enclosing caller has no callers; an unused twin cannot satisfy differential or consumer-switch retirement. |
| 41 | `galerina-core-network/src/index.ts#isUnsafeNetworkBackend` | `BLOCKED` | The source consumes a nominal record with optional Boolean state; the physical frontend has no exact `Option<Bool>` record boundary. |
| 42 original | `galerina-cpu-kernels/src/index.ts#requiresLowBitKernel` | `SUPERSEDED_BY_EXISTING_FUNGI` | Slice 29 already owns the Fungi asset, differential test and physical proof. |
| 42 replacement | `galerina-core-tasks/src/check-permissions.ts#isSafeEnvironmentName` | `BLOCKED` | The open regular-language decision needs unbounded code-unit iteration, text length and character access absent from the selected physical profile. |
| 43 | `galerina-devtools-pci/src/pci-checker.ts#containsCardKeyword` | `BLOCKED` | Physical text containment exists, but exact JavaScript case folding does not; the physical text domain is also narrower. |

## Product-owner verification

The current code graph independently confirms the exact symbols and caller
sets. It also confirms that `selectNetworkBackend`, the sole caller of Slice
41, has only a package-test caller, while replacement Slice 42 remains live
through `checkTaskPermissions`, `runTask` and the CLI task command.

The selected SLIDE frontend independently confirms:

- fixed external records exist, but its parser admits `Option<Int>` only, not
  the required `Option<Bool>` field;
- the scalar statement grammar has no `while`;
- text members include trim, contains, prefix, suffix and slice, but no text
  length, character accessor or case-fold operation;
- physical String admission remains bounded and well-formed, narrower than an
  unbounded JavaScript String.

Owning package regression lanes remain green: core-network **192/192**,
core-tasks **9/9**, and devtools-pci **29/29**. These counts prove regression
health only, not conversion parity or retirement authority.

## Classification and queue rules

No blocked scope enters `governance/conversion-queue-decisions.json`, whose
explicit rows are candidate authority. The generated default classification
remains fail-closed. The live register and this exact design carry the named
blocker evidence until a future profile or source-contract change triggers a
fresh dossier.

A physical refusal is not conversion success. A frontend-only probe is not a
physical receipt. A scalar convenience wrapper cannot prove a wider record or
optional-field ABI. A dead symbol follows deletion adjudication rather than
receiving a reference twin.

## Skill-close review

`NO_SKILL_UPDATE` for this wave. The two public skills already require exact
record boundaries, explicit `Option<T>`, full source-domain comparison, and
separation of frontend checking from physical proof. The finite-membership
versus open regular-language distinction remains recorded here until it is
proved reusable across another slice; no slice-specific counts or profile
limits are copied into a public skill.
