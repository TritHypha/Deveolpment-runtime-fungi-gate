@version 1

/// Bounded pure leaf for the SECRETS_GATEWAY_WIT text constant in types.ts.
/// TypeScript shadow: packages-ts/galerina-ext-secrets-vault/src/types.ts#SECRETS_GATEWAY_WIT
/// Unicode escapes preserve the source CRLF framing exactly. Vault, TPM,
/// credential custody, and WIT host/provider effects remain manual boundaries.

pure flow secretsGatewayWit() -> String
contract { intent { "Return the exact vendor-agnostic secrets gateway WIT interface text." } }
{
  return "\u{000D}\u{000A}interface secrets-gateway {\u{000D}\u{000A}  type secret-handle = u32;\u{000D}\u{000A}  provide-secret-buffer: func(id: string, stage_slot: u32) -> list<u8>;\u{000D}\u{000A}}\u{000D}\u{000A}"
}
