@version 1
/// Secret-section record assembly readiness.
pure flow sporeSecretSectionStatusCore(coordReady: Bool, payloadReady: Bool) -> String {
  if coordReady {
    if payloadReady { return "secret_section" }
    return "payload_refused"
  }
  return "coord_refused"
}
