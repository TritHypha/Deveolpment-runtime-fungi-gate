[package]
name = "galerina-requirement-launcher"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "deny", check-cfg = ["cfg(test_contract)"] }
