@version 1
pure flow add(a: Int, b: Int) -> Int {
  return a + b
}

secure flow main() -> Result<Void, Error>
contract { intent { "Add two integers and print the deterministic result." } }
{
  let total: Int = add(2, 3)
  console.log("sum: " . total)
  return Ok()
}
