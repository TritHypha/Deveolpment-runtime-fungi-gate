@version 1
// Project names and entry paths are deployment-manifest fields, not executable
// top-level language declarations.
secure flow main() -> Result<Void, Error>
contract { intent { "Print the AI-context demonstration greeting." } }
{
  print("AI context example")
  return Ok()
}
