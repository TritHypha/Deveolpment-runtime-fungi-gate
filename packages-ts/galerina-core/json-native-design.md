# JSON-native design — moved

This document's canonical home is [`docs/json-native-design.md`](docs/json-native-design.md).

An identical copy previously sat here and at that path. Two copies of one document drift the
moment either is edited, so this location is now a pointer and the content lives in `docs/`
with the rest of the package's documentation.
