# Canonical benchmark contracts and evidence are hashed as exact bytes.
# Disable line-ending conversion so a fresh Windows checkout preserves the
# reviewed LF byte stream and cannot invalidate or silently remint its pins.
packages-galerina/galerina-devtools-benchmarks/contracts/*.json -text
packages-galerina/galerina-devtools-benchmarks/evidence/*.json -text
